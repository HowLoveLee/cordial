function passwordCheck() {
    const username = document.getElementById('studentEmail');
    const password = doucment.getElementById('studentPassword');

    if (password === username.substring(0, 10)) {
        console.log("hey");
        preventDefault(e);
    }
}
